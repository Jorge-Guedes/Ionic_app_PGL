import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.holamundoionic',
  appName: 'holamundoionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
